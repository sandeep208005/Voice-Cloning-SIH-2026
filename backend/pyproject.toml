[tool.vercel]
entrypoint = "main:app"

[project]
name = "deepshield-backend"
version = "2.0.0"
description = "DeepShield AI Multimodal Impersonation Defense Backend"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
